designa Server-Client application starting from the already define classes in the 3 projects:
HandShakeLib: contains all the common features,
HandShakeClient: contains the client application,
HandShakeServer: contains the server application.

Step 1. 
le the application work, completing the implementation for the classes HandShakeClient and HandShakeServer

Step 2. fix this:
let the event NewMessage readable only withint the receiving context